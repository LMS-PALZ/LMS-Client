# Admin config lives in the repo root netlify.toml (admin site deploys from repo root).
# Student site uses apps/student/netlify.toml instead.
#
# Admin Netlify UI: base empty, branch dev-admin, all other build fields empty.
