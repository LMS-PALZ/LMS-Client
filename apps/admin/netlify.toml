# Admin portal (branch: dev-admin)
#
# Netlify UI:
#   Base directory:      (empty = repo root)
#   Package directory:   apps/admin
#   Build command:       (empty)
#   Publish directory:   (empty — set via publish below, relative to repo root)
#   Functions directory: (empty)

[build]
  command = "npm ci && npm run build -w @ssu/admin"
  publish = "apps/admin/.next"

[build.environment]
  NODE_VERSION = "24.14.0"

[[plugins]]
  package = "@netlify/plugin-nextjs"
