# Monorepo — do NOT add [build] settings here.
#
# Each Netlify site reads its own config via Package directory in the UI:
#   Student site → Package directory: apps/student  → apps/student/netlify.toml
#   Admin site   → Package directory: apps/admin    → apps/admin/netlify.toml
#
# If admin [build] lives here, the student site (Base = /) will deploy admin instead.
