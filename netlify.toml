# Monorepo — do not add [build] settings here.
#
# Each app has its own netlify.toml. Set Package directory in the Netlify UI:
#   Student site → apps/student
#   Admin site   → apps/admin
#
# Leave Base directory empty (repo root), and clear Build / Publish / Functions
# overrides in the Netlify UI so each app's netlify.toml is used.
