# Monorepo root — copy per-app examples instead of using this file for Next.js.
# Student: apps/student/.env.local  (see apps/student/.env.example)
# Admin:   apps/admin/.env.local
# Tutor:   apps/tutor/.env.local
